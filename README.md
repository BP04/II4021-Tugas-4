# II4021-Tugas-4

## Menjalankan Dengan Docker

Build image:

```bash
docker compose build
```

Jalankan server:

```bash
docker compose up -d server
```

Jalankan client:

```bash
docker compose run --rm client --help
```