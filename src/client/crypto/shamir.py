from __future__ import annotations

from ast import Pass
from typing import Any


def split_secret(secret: bytes) -> list[tuple[int, bytes]]:
    pass


def join_shares(shares: list[tuple[int, bytes]]) -> bytes:
    pass